export type EnvConfig = typeof envConfig;

export const envConfig = {
    AUTH : {
        URL : "https://authenticate.pearlapis.com/",
        VERSION : "v1"
    },
    ACCOUNT: {
        URL: "https://account.pearlabyss.com/en-us/"
    }
}